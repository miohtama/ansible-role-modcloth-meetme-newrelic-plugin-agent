# vim:filetype=upstart
# {{ ansible_managed }}

description "Meetme New Relic plugin agent"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 3 30

exec newrelic-plugin-agent -f -c {{ meetme_newrelic_plugin_agent_conf_path }}
